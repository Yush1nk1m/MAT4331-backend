version: "3.8"

services:
  basetalk-backend:
    image: kys010306/mat4331-basetalk-crawler:latest
    container_name: basetalk-crawler
    ports:
      - "8079:8079"
    env_file:
      - .env
    restart: always
